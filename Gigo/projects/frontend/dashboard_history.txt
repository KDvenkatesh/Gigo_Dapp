--- replace_file_content ---
Target:
"    const [refundedTxId, setRefundedTxId] = useState<string | null>(null)\n    const timeoutIntervalRef = useRef<ReturnType<typeof setInterval> | null>(null)\n\n    useEffect(() => {"
Replacement:
"    const [refundedTxId, setRefundedTxId] = useState<string | null>(null)\n    const timeoutIntervalRef = useRef<ReturnType<typeof setInterval> | null>(null)\n\n    interface SurgeInfo {\n       weatherMultiplier: number;\n       trafficMultiplier: number;\n       trafficCondition: string;\n    }\n    const [surgeInfo, setSurgeInfo] = useState<SurgeInfo>({\n       weatherMultiplier: 1.0,\n       trafficMultiplier: 1.0,\n       trafficCondition: 'MODERATE'\n    })\n    const [isEstimatingSurge, setIsEstimatingSurge] = useState(false)\n\n    useEffect(() => {"
--- replace_file_content ---
Target:
"    const distanceKm = useMemo(\n       () => calculateDistanceKm(pickupLocation, destinationLocation),\n       [pickupLocation, destinationLocation],\n    )\n    const estimatedFare = useMemo(\n       () => BigInt(Math.max(100000, Math.round(distanceKm * 1000000 * ride.selectedVehicle.multiplier * 0.18))),\n       [distanceKm, ride.selectedVehicle.multiplier],\n    )\n    const discountedFare = useMemo(\n       () => passData.applyDiscount(estimatedFare),\n       [estimatedFare, passData.applyDiscount],\n    )\n    const hasActivePass = Boolean(passData.activePass && passData.activePass.isActive)"
Replacement:
"    const distanceKm = useMemo(\n       () => calculateDistanceKm(pickupLocation, destinationLocation),\n       [pickupLocation, destinationLocation],\n    )\n    const estimatedFare = useMemo(\n       () => {\n          let base = Math.max(100000, Math.round(distanceKm * 1000000 * ride.selectedVehicle.multiplier * 0.18));\n          base = Math.round(base * surgeInfo.weatherMultiplier * surgeInfo.trafficMultiplier);\n          return BigInt(base);\n       },\n       [distanceKm, ride.selectedVehicle.multiplier, surgeInfo],\n    )\n    const discountedFare = useMemo(\n       () => passData.applyDiscount(estimatedFare),\n       [estimatedFare, passData.applyDiscount],\n    )\n    const hasActivePass = Boolean(passData.activePass && passData.activePass.isActive)\n\n    useEffect(() => {\n       const fetchSurge = async () => {\n          if (pickupLocation.lat === 0 || destinationLocation.lat === 0) return;\n          setIsEstimatingSurge(true);\n          try {\n             let BACKEND_URL = import.meta.env.VITE_BACKEND_URL || 'http://localhost:3001'\n             if (typeof window !== 'undefined') {\n                if (window.location.hostname === 'localhost') {\n                   BACKEND_URL = 'http://localhost:3001'\n                } else if (BACKEND_URL.includes('localhost')) {\n                   BACKEND_URL = 'https://gigo-dapp.onrender.com'\n                }\n             }\n             const response = await fetch(`${BACKEND_URL}/api/rides/estimate-surge?pickupLat=${pickupLocation.lat}&pickupLng=${pickupLocation.lng}&dropLat=${destinationLocation.lat}&dropLng=${destinationLocation.lng}`);\n             if (response.ok) {\n                const data = await response.json();\n                setSurgeInfo({\n                   weatherMultiplier: data.weatherMultiplier,\n                   trafficMultiplier: data.trafficMultiplier,\n                   trafficCondition: data.trafficCondition\n                });\n             }\n          } catch (err) {\n             console.error('Failed to fetch surge
<truncated 343 bytes>
--- replace_file_content ---
Target:
"                              {/* Price Prediction Bento Block (Glassmorphic) */}\n                              <div className=\"col-span-12 sm:col-span-6 glass-container glass-container--rounded\">\n                                 <div className=\"glass-filter\"></div>\n                                 <div className=\"glass-overlay\"></div>\n                                 <div className=\"glass-specular\"></div>\n                                 <div className=\"glass-content p-6 flex flex-col justify-between\">\n                                    <PricePrediction\n                                       pickup={pickupLocation.label}\n                                       destination={destinationLocation.label}\n                                       baseFare={Number(hasActivePass ? discountedFare : estimatedFare) / 1000000}\n                                       flat\n                                    />\n                                 </div>\n                              </div>\n\n                              {/* Trip summary Bento Block (Glassmorphic) */}\n                              <div className=\"col-span-12 sm:col-span-6 glass-container glass-container--rounded\">\n                                 <div className=\"glass-filter\"></div>\n                                 <div className=\"glass-overlay\"></div>\n                                 <div className=\"glass-specular\"></div>\n                                 <div className=\"glass-content p-6 flex flex-col justify-between\">\n                                    <p className=\"text-[10px] font-black uppercase tracking-widest opacity-50 border-b border-current/10 pb-2\">Trip Summary</p>\n                                    <div className=\"grid grid-cols-3 gap-2 pt-4 flex-1\">\n                                       <div className=\"text-center sm:text-left\">\n                                          <p className=\"text-[9px] font-black uppercase tracking-widest opacity-50\">Distance</p>\n                                          <
<truncated 2885 bytes>
Replacement:
"                              {/* Price Prediction Bento Block (Glassmorphic) */}\n                              <div className=\"col-span-12 sm:col-span-6 glass-container glass-container--rounded\">\n                                 <div className=\"glass-filter\"></div>\n                                 <div className=\"glass-overlay\"></div>\n                                 <div className=\"glass-specular\"></div>\n                                 <div className=\"glass-content p-6 flex flex-col justify-between\">\n                                    <PricePrediction\n                                       pickup={pickupLocation.label}\n                                       destination={destinationLocation.label}\n                                       baseFare={Number(hasActivePass ? discountedFare : estimatedFare) / 1000000}\n                                       flat\n                                    />\n                                 </div>\n                              </div>\n\n                              {/* Surge Notifications (Conditional) */}\n                              {(surgeInfo.weatherMultiplier > 1.0 || surgeInfo.trafficMultiplier > 1.0) && (\n                                 <div className=\"col-span-12 glass-container glass-container--rounded overflow-hidden\">\n                                    <div className=\"glass-filter\"></div>\n                                    <div className=\"glass-overlay bg-gradient-to-r from-orange-500/10 to-amber-500/10\"></div>\n                                    <div className=\"glass-specular\"></div>\n                                    <div className=\"glass-content p-4 space-y-2\">\n                                       {surgeInfo.weatherMultiplier > 1.0 && (\n                                          <div className=\"flex items-start gap-3\">\n                                             <div className=\"mt-0.5 text-blue-400\">🌧️</div>\n                                             <div>\n                                            
<truncated 5823 bytes>
--- replace_file_content ---
Target:
"export function CustomerDashboard({ ride, onBack, onSwitchRole }: { ride: RideHook; onBack: () => void; onSwitchRole: (role: AppRole) => void }) {\n   const { location, isLocating, locationError } = useGeolocation()\n   const passData = useAlgorandAssets()\n   const [tab, setTab] = useState<'book' | 'history' | 'passes'>('book')"
Replacement:
"export function CustomerDashboard({ ride, onBack, onSwitchRole }: { ride: RideHook; onBack: () => void; onSwitchRole: (role: AppRole) => void }) {\n   const { activeAddress, signTransactions } = useWallet()\n   const { location, isLocating, locationError } = useGeolocation()\n   const passData = useAlgorandAssets()\n   const [outstandingDebt, setOutstandingDebt] = useState<number>(0)\n   const [isClearingDebt, setIsClearingDebt] = useState(false)\n   const [tab, setTab] = useState<'book' | 'history' | 'passes'>('book')"
--- replace_file_content ---
Target:
"    useEffect(() => {\n      if (!pickupTouched) {\n         setPickupInput(location.label)\n         setPickupLocation(location)\n      }\n    }, [location, pickupTouched])"
Replacement:
"    useEffect(() => {\n      if (!pickupTouched) {\n         setPickupInput(location.label)\n         setPickupLocation(location)\n      }\n    }, [location, pickupTouched])\n\n    const fetchCustomerDebt = useCallback(async () => {\n      if (!activeAddress) return;\n      try {\n         let BACKEND_URL = import.meta.env.VITE_BACKEND_URL || 'http://localhost:3001'\n         if (typeof window !== 'undefined') {\n            if (window.location.hostname === 'localhost') {\n               BACKEND_URL = 'http://localhost:3001'\n            } else if (BACKEND_URL.includes('localhost')) {\n               BACKEND_URL = 'https://gigo-dapp.onrender.com'\n            }\n         }\n         const res = await axios.get(`${BACKEND_URL}/api/customers/${activeAddress}`)\n         setOutstandingDebt(res.data.outstandingDebt || 0)\n      } catch (e) {\n         console.error('Failed to fetch customer debt:', e)\n      }\n    }, [activeAddress])\n\n    useEffect(() => {\n       void fetchCustomerDebt()\n    }, [fetchCustomerDebt, activeRide?.status]) // Re-fetch when ride status changes (e.g., finishes)"
--- replace_file_content ---
Target:
"                 setSurgeInfo({\n                    weatherMultiplier: data.weatherMultiplier,\n                    trafficMultiplier: data.trafficMultiplier,\n                    trafficCondition: data.trafficCondition\n                 })\n              }\n           } catch (error) {\n              console.error('Failed to fetch surge info', error)\n           } finally {\n              setIsEstimatingSurge(false);\n           }\n        }\n        void fetchSurge()\n     }, [pickupLocation, destinationLocation])\n\n    const activeRide = ride.customerRides.find("
Replacement:
"                 setSurgeInfo({\n                    weatherMultiplier: data.weatherMultiplier,\n                    trafficMultiplier: data.trafficMultiplier,\n                    trafficCondition: data.trafficCondition\n                 })\n              }\n           } catch (error) {\n              console.error('Failed to fetch surge info', error)\n           } finally {\n              setIsEstimatingSurge(false);\n           }\n        }\n        void fetchSurge()\n     }, [pickupLocation, destinationLocation])\n\n    const handleClearDebt = async () => {\n      if (!activeAddress || outstandingDebt <= 0) return;\n      try {\n         setIsClearingDebt(true);\n         const suggestedParams = await algodClient.getTransactionParams().do();\n         const treasuryAddress = import.meta.env.VITE_TREASURY_ADDRESS || '6C2QYZ2JOSL3XMMF3Z2JWWZ23EGB6N2X3T2YI2J37XUXU3YIZW276BOGU4';\n         const assetId = Number(import.meta.env.VITE_GIGC_ASSET_ID || '763011769');\n         \n         const transferTxn = algosdk.makeAssetTransferTxnWithSuggestedParamsFromObject({\n            from: activeAddress,\n            to: treasuryAddress,\n            amount: outstandingDebt,\n            assetIndex: assetId,\n            suggestedParams,\n         });\n\n         const encodedTxn = algosdk.encodeUnsignedTransaction(transferTxn);\n         const signedTxns = await signTransactions([encodedTxn]);\n         const { txId } = await algodClient.sendRawTransaction(signedTxns).do();\n         \n         // Verify and clear\n         let BACKEND_URL = import.meta.env.VITE_BACKEND_URL || 'http://localhost:3001';\n         if (typeof window !== 'undefined') {\n            if (window.location.hostname === 'localhost') {\n               BACKEND_URL = 'http://localhost:3001';\n            } else if (BACKEND_URL.includes('localhost')) {\n               BACKEND_URL = 'https://gigo-dapp.onrender.com';\n            }\n         }\n\n         // Poll backend until indexer catches up\n         let cleared = false;\n         for (let i = 0; i
<truncated 913 bytes>
--- replace_file_content ---
Target:
"                  {/* Booking form — always scrollable */}\n                  <div className=\"flex-1 min-h-0 overflow-y-auto custom-scrollbar bg-transparent pr-1\">\n                     <div className=\"mx-auto w-full max-w-3xl space-y-5 py-3 pb-20\">\n\n                        {locationError && (\n                           <div className=\"rounded-2xl border border-amber-500/15 bg-amber-500/[0.06] p-4 text-sm text-amber-200/80\">\n                              {locationError}\n                           </div>\n                        )}\n\n                        {/* Pickup + Destination (Glassmorphic Container) */}\n                        <div className=\"glass-container glass-container--rounded glass-container--large mb-4\">\n                           <div className=\"glass-filter\"></div>\n                           <div className=\"glass-overlay\"></div>\n                           <div className=\"glass-specular\"></div>\n                           <div className=\"glass-content p-6 space-y-4\">"
Replacement:
"                  {/* Booking form — always scrollable */}\n                  <div className=\"flex-1 min-h-0 overflow-y-auto custom-scrollbar bg-transparent pr-1\">\n                     <div className=\"mx-auto w-full max-w-3xl space-y-5 py-3 pb-20\">\n\n                        {outstandingDebt > 0 ? (\n                           <div className=\"glass-container glass-container--rounded glass-container--large mb-4 overflow-hidden\">\n                              <div className=\"glass-filter\" style={{ backdropFilter: 'blur(20px) saturate(120%)' }}></div>\n                              <div className=\"glass-overlay bg-gradient-to-b from-red-500/10 to-transparent\"></div>\n                              <div className=\"glass-specular\"></div>\n                              <div className=\"glass-content p-6 text-center space-y-5\">\n                                 <div className=\"mx-auto flex h-14 w-14 items-center justify-center rounded-full bg-red-500/20 text-red-400\">\n                                    <Wallet className=\"h-7 w-7\" />\n                                 </div>\n                                 <div>\n                                    <h3 className=\"text-lg font-bold text-white mb-2\">Outstanding Balance</h3>\n                                    <p className=\"text-sm text-white/70\">\n                                       You have an unpaid balance of <span className=\"font-bold text-red-400\">{ride.formatAlgoAmount(outstandingDebt)}</span> from wait fees or traffic delays on a previous ride.\n                                    </p>\n                                    <p className=\"text-xs text-white/50 mt-2\">\n                                       Please clear this balance to unlock ride booking.\n                                    </p>\n                                 </div>\n                                 <button\n                                    onClick={handleClearDebt}\n                                    disabled={isClearingDebt}\n                               
<truncated 1563 bytes>
--- replace_file_content ---
Target:
"                                 </button>\n                              </div>\n                           </div>\n                        )}\n\n                        {refundedTxId && ("
Replacement:
"                                 </button>\n                              </div>\n                           </>\n                        )}\n\n                        {refundedTxId && ("
--- multi_replace_file_content ---
